package com.test;

from java++ import expressions.equalityoperator;

public class TestEqualityOperator {
	public static void main(String[] args) {
		String test = new String("test");
		
		assert test == "test";
		assert test != null;
		assert test == test;
		assert test is test;
		assert test is! null;
	}
}
