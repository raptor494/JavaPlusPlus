package com.test;

from java++ import expressions.elvisoperator;

import java.util.Random;

public class TestElvisOperator {
	public static void main(String[] args) {
		var rand = new Random();
		String str = rand.nextBoolean()? null : "test";
		
		assert "test".equals(str ?: "test");
	}
}
